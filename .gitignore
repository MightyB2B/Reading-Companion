# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dictionary build: the 23MB Webster's source and the 40MB database it
# produces are both regenerable with `node scripts/build-dictionary.mjs`.
.cache
src-tauri/resources/dict.sqlite

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Written by the server experiment on the other branch; main has no server.
.env
/library/

# Cargo output from the abandoned workspace layout; main builds into
# src-tauri/target. This one is left over and must never be committed.
/target/
